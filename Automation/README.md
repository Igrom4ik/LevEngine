# 🏗️ HuyEngine - Система автоматизации сборки v2.0

Современная система автоматизации сборки с интерактивным меню и поддержкой различных IDE.

> 📚 Упрощённый индекс документации: [docs/README.md](../docs/README.md)

## ✨ Основные возможности

- 🎮 **Интерактивное меню** - удобное управление без запоминания команд
- 🛠️ **Поддержка IDE** - Visual Studio, CLion, VSCode, Command Line
- ⚡ **Быстрая сборка** - Ninja, MSBuild, Unix Makefiles
- 📁 **Умные папки** - отдельные папки для каждой конфигурации
- 🔍 **Автообнаружение** - автоматический поиск CMake, Ninja, Visual Studio
- 🎨 **Цветной вывод** - наглядное отображение процесса
- ⚙️ **Гибкая настройка** - пользовательская конфигурация
- 📚 **Документация** - полное руководство на русском языке

## ⌨️ Горячие клавиши в CLion

**Настройка External Tool для запуска Build Menu:**

- 🚀 [docs/CLION_HOTKEYS.md](../docs/CLION_HOTKEYS.md) — одна простая рабочая конфигурация

**Диагностика:**

```batch
.\diagnose_hotkeys.bat   # Подробная диагностика
.\check_hotkeys.bat      # Быстрая проверка
```

## 🚀 Быстрый старт

### Интерактивный режим (самый простой)

```batch
build.bat
```

### Командная строка

```batch
# Полная пересборка
build.bat rebuild

# Сборка
build.bat build

# Запуск
build.bat run
```

## 📖 Документация

| Документ                                           | Описание                                      |
|----------------------------------------------------|-----------------------------------------------|
| [BUILD_QUICKSTART.md](../docs/BUILD_QUICKSTART.md) | ⚡ Быстрый старт - начните здесь!              |
| [BUILD_SYSTEM_README.md](BUILD_SYSTEM_README.md)   | 📘 Полное руководство по системе              |
| [BUILD_EXAMPLES.md](BUILD_EXAMPLES.md)             | 💡 Примеры использования для разных сценариев |
| [BUILD_FAQ.md](BUILD_FAQ.md)                       | ❓ Ответы на частые вопросы                    |
| [CLION_HOTKEYS.md](../docs/CLION_HOTKEYS.md)       | ⌨️ Хоткей для запуска Build Menu в CLion      |

## 🎯 Популярные сценарии

### Первая сборка проекта

```batch
Automation\build.bat
# Выберите: Сборка → Rebuild
```

### Для разработчиков Visual Studio

```batch
Automation\build.bat generate --ide vs --build-system msbuild
start vs-build\HuyEngine.sln
```

### Для разработчиков CLion

```batch
Automation\build.bat -i clion -c debug generate
# Откройте проект в CLion
```

### Быстрая сборка с Ninja

```batch
Automation\build.bat -b ninja -c release rebuild
```

### Форматирование кода

```batch
Automation\build.bat format
```

### Очистка всех артефактов

```batch
Automation\build.bat clean-all
```

## 📁 Структура папок

```
HuyEngine/
├── CMakeLists.txt                # Корневая конфигурация CMake
├── CMakePresets.json             # CMake пресеты для прямого использования
├── Automation/
│   ├── build.bat                 # Главный launcher для Windows
│   ├── build_menu.bat            # Launcher интерактивного меню
│   ├── automation_new.py         # Главный скрипт автоматизации
│   ├── build_config.py           # Конфигурация сборки
│   ├── build_actions.py          # Действия сборки
│   ├── build_menu.py             # Интерактивное меню
│   ├── user_config.template.py   # Шаблон пользовательской конфигурации
│   ├── BUILD_SYSTEM_README.md    # Полная документация
│   ├── BUILD_EXAMPLES.md         # Примеры использования
│   ├── BUILD_FAQ.md              # Часто задаваемые вопросы
│   └── CMAKE/
│       ├── CmakeHelpers.cmake    # CMake хелперы
│       └── Toolchains/           # Тулчейны компиляторов
├── App/                          # Приложение
├── Engine/                       # Библиотека движка
├── docs/                         # Документация
└── [папки сборки создаются автоматически]
    ├── cmake-build-debug/        # CLion Debug
    ├── cmake-build-release/      # CLion Release
    └── vs-build/                 # Visual Studio (опционально)
```

## ⚙️ Пользовательская конфигурация

Создайте `Automation/user_config.py` для настройки под себя:

```powershell
# Скопируйте шаблон (PowerShell)
Copy-Item -Path "Automation/user_config.template.py" -Destination "Automation/user_config.py"
```

Затем отредактируйте `Automation/user_config.py` и задайте желаемые параметры, например:

```python
DEFAULT_IDE = "vs"
DEFAULT_BUILD_SYSTEM = "msbuild"
DEFAULT_CONFIGURATION = "release"

# Пользовательские пути (если нужно)
USER_CMAKE_PATH = r"D:\Tools\CMake\bin\cmake.exe"
USER_NINJA_PATH = r"D:\Tools\Ninja\ninja.exe"

# Дополнительные CMake флаги
CUSTOM_CMAKE_FLAGS = ["-DENABLE_TESTING=ON"]
```

## 🔍 Автоматическое обнаружение

Система автоматически находит:

- ✅ CMake (Program Files, chocolatey, PATH)
- ✅ Ninja (chocolatey, Program Files, PATH)
- ✅ Visual Studio 2019/2022 (Community, Professional, Enterprise)
- ✅ MSVC компиляторы и vcvarsall.bat

При запуске отображается статус:

```
Статус инструментов:
  CMake:  ✓ Найден
  Ninja:  ✓ Найден
  MSVC:   ✓ Найден
```

# HuyEngine Build Automation

Теперь поддерживается выбор компилятора (toolchain) и пресетов CMake.

- Поддерживаемые тулчейны: MSVC (cl), Clang-CL, MinGW (GCC)
- Генератор по умолчанию: Ninja

Примеры:

- Меню:
  python Automation/automation_new.py

- Генерация и сборка с clang-cl:
  python Automation/automation_new.py generate -b ninja -c debug -t clang-cl
  python Automation/automation_new.py build -b ninja -c debug -t clang-cl

- Генерация и сборка с MinGW:
  python Automation/automation_new.py generate -b ninja -c release -t mingw
  python Automation/automation_new.py build -b ninja -c release -t mingw

Также доступны CMakePresets.json:

- Конфигурирование:
  cmake --preset ninja-clangcl-debug
  cmake --build --preset build-debug-clangcl

## 🆚 Сравнение со старой системой

| Функция              | Старая       | Новая                |
|----------------------|--------------|----------------------|
| Интерактивное меню   | ❌            | ✅                    |
| Поддержка IDE        | ❌            | ✅                    |
| Выбор системы сборки | Только Ninja | Ninja, MSBuild, Make |
| Отдельные папки      | ❌            | ✅                    |
| Автообнаружение      | ❌            | ✅                    |
| Цветной вывод        | ❌            | ✅                    |
| Документация         | Минимальная  | Полная на русском    |
| Настройка            | Править код  | user_config.py       |

## 🐛 Решение проблем

### Инструменты не найдены

```batch
# Установите через chocolatey
choco install cmake ninja

# Или создайте user_config.py с путями
```

### Ошибки компиляции

```batch
# Полная пересборка
build.bat clean-all
build.bat rebuild

# С подробным выводом
build.bat --verbose build
```

### Python не найден

```batch
# Установите Python
choco install python

# Убедитесь что Python в PATH
python --version
```

Больше решений в [BUILD_FAQ.md](BUILD_FAQ.md).

## 🤝 Для разработчиков

### Добавление нового действия

1. Добавьте функцию в `build_actions.py`
2. Добавьте вызов в `build_menu.py`
3. Добавьте в argparse в `automation_new.py`

### Тестирование

```batch
# Проверка help
build.bat --help

# Тестирование команды
build.bat generate --verbose

# Тестирование меню
build.bat
```

## 📜 Лицензия

Этот проект является частью HuyEngine.

## 🎉 Быстрые ссылки

| Что нужно        | Команда                                            |
|------------------|----------------------------------------------------|
| 🚀 Начать работу | `build.bat`                                        |
| 📖 Документация  | [BUILD_QUICKSTART.md](../docs/BUILD_QUICKSTART.md) |
| 💡 Примеры       | [BUILD_EXAMPLES.md](BUILD_EXAMPLES.md)             |
| ❓ Вопросы        | [BUILD_FAQ.md](BUILD_FAQ.md)                       |
| ⚙️ Настройка     | `Automation/user_config.template.py`               |
| 🆘 Помощь        | `build.bat --help`                                 |

## 📝 История версий

### v2.0 (2025-01-13)

- ✨ Интерактивное меню
- ✨ Поддержка Visual Studio, CLion, VSCode
- ✨ Выбор системы сборки (Ninja, MSBuild, Make)
- ✨ Отдельные папки для конфигураций
- ✨ Автоматическое обнаружение инструментов
- ✨ Пользовательская конфигурация
- ✨ Полная документация на русском
- ✨ Цветной вывод

### v1.0 (ранее)

- Базовая автоматизация с Ninja
- Поддержка командной строки

---

**Начните прямо сейчас:**

```batch
cd C:\DEVOPS\HuyEngine
build.bat
```

**Приятной работы!** 🎯
