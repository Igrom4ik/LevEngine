# HuyEngine - Система автоматизации сборки

Улучшенная система автоматизации сборки с поддержкой различных IDE и систем сборки.

## 🚀 Возможности

- **Интерактивное меню** для удобного управления
- **Поддержка различных IDE**: Visual Studio, CLion, VSCode, Command Line
- **Различные системы сборки**: Ninja, MSBuild, Unix Makefiles
- **Множество конфигураций**: Debug, Release, RelWithDebInfo, MinSizeRel
- **Управление папками сборки**: отдельные папки для каждой конфигурации
- **Автоматическое обнаружение инструментов**: CMake, Ninja, Visual Studio
- **Форматирование кода** с помощью clang-format
- **Поддержка командной строки** для автоматизации и CI/CD

## 📋 Требования

- **Python 3.6+**
- **CMake 3.31.6+**
- **Компилятор C++**:
    - Visual Studio 2019/2022 (для Windows)
    - GCC/Clang (для Linux/macOS)
- **Ninja** (опционально, рекомендуется)
- **clang-format** (опционально, для форматирования)

## 🎮 Использование

### Интерактивный режим (рекомендуется)

Просто запустите скрипт без параметров:

```batch
# Windows
build_new.bat

# Или напрямую
python automation_new.py
```

Вы увидите интерактивное меню:

```
======================================================================
                HuyEngine - Build Automation Menu
======================================================================

Текущая конфигурация:
  IDE:             Manual (Command Line)
  Система сборки:  Ninja
  Конфигурация:    Debug
  Платформа:       x64
  Папка сборки:    build

ГЛАВНОЕ МЕНЮ

  1. Конфигурация
  2. Сборка
  3. Инструменты
  0. Выход

Выберите пункт меню:
```

### Режим командной строки

#### Основные команды

```batch
# Генерация проекта
build_new.bat generate

# Сборка проекта
build_new.bat build

# Пересборка (clean + generate + build)
build_new.bat rebuild

# Очистка текущей папки сборки
build_new.bat clean

# Очистка всех папок сборки
build_new.bat clean-all

# Запуск приложения
build_new.bat run

# Форматирование кода
build_new.bat format
```

#### С параметрами

```batch
# Генерация для CLion в режиме Debug
build_new.bat generate --ide clion --config debug

# Сборка с Ninja в режиме Release
build_new.bat build --build-system ninja --config release

# Пересборка для Visual Studio с MSBuild
build_new.bat rebuild --ide vs --build-system msbuild

# Короткая форма команд
build_new.bat -i clion -c debug generate
build_new.bat -b ninja -c release build
```

#### Опции

```batch
-i, --ide [IDE]              Выбор IDE
    vs, visual-studio        Visual Studio
    clion                    CLion  
    vscode                   Visual Studio Code
    manual                   Командная строка

-b, --build-system [SYSTEM]  Система сборки
    ninja                    Ninja
    msbuild                  MSBuild (Visual Studio)
    make                     Unix Makefiles

-c, --config [CONFIG]        Конфигурация
    debug                    Debug
    release                  Release
    relwithdebinfo           RelWithDebInfo
    minsizerel               MinSizeRel

-p, --platform [PLATFORM]    Платформа
    x64                      64-bit (по умолчанию)
    win32                    32-bit
    arm64                    ARM 64-bit

--verbose                    Подробный вывод
--no-clean                   Не очищать перед сборкой
```

## 📁 Структура папок сборки

Система автоматически создает отдельные папки в зависимости от IDE и конфигурации:

- **CLion**: `cmake-build-debug`, `cmake-build-release`, и т.д.
- **Visual Studio**: `vs-build`
- **Manual/Other**: `build`

Это позволяет иметь несколько конфигураций одновременно без конфликтов.

## 🔧 Примеры использования

### Для разработчиков Visual Studio

```batch
# Настройка проекта для Visual Studio с MSBuild
build_new.bat generate --ide vs --build-system msbuild --config debug

# Сборка
build_new.bat build --ide vs --build-system msbuild --config release

# Или используйте интерактивное меню
build_new.bat
# Выберите: Конфигурация -> Visual Studio -> MSBuild
```

### Для разработчиков CLion

```batch
# CLion использует cmake-build-* папки
build_new.bat generate --ide clion --config debug

# Быстрая сборка
build_new.bat -i clion -c debug build
```

### Для быстрой сборки с Ninja

```batch
# Ninja - самый быстрый вариант
build_new.bat rebuild --build-system ninja --config release
```

### Очистка перед релизом

```batch
# Удалить все артефакты сборки
build_new.bat clean-all

# Чистая сборка Release
build_new.bat rebuild --config release
```

### Форматирование кода

```batch
# Применить clang-format ко всем исходникам
build_new.bat format
```

## 🛠️ Конфигурация

Основные настройки находятся в `build_config.py`:

```python
class BuildConfig:
    # Имя исполняемого файла
    EXECUTABLE_NAME = "HuyEngine.exe"

    # Директории для форматирования
    SOURCE_DIRS = ["Engine", "main.cpp"]

    # Текущие настройки (изменяются через меню или аргументы)
    current_ide = IDE.MANUAL
    current_build_system = BuildSystem.NINJA
    current_configuration = Configuration.DEBUG
    current_platform = Platform.X64
```

## 🔍 Автоматическое обнаружение инструментов

Система автоматически ищет установленные инструменты:

- **CMake**: в Program Files, chocolatey, или PATH
- **Ninja**: в chocolatey, Program Files, или PATH
- **Visual Studio**: 2022 и 2019 (Community, Professional, Enterprise)

При запуске отображается статус найденных инструментов:

```
Статус инструментов:
  CMake:  ✓ Найден
          C:\Program Files\CMake\bin\cmake.exe
  Ninja:  ✓ Найден
          C:\ProgramData\chocolatey\bin\ninja.exe
  MSVC:   ✓ Найден
          C:\Program Files\Microsoft Visual Studio\2022\Community\...
```

## 🎯 Интеграция с CI/CD

Для автоматизации в CI/CD используйте режим командной строки:

```yaml
# Пример для GitHub Actions
- name: Generate project
  run: python Automation/automation_new.py generate --config release

- name: Build project
  run: python Automation/automation_new.py build --config release

- name: Run tests
  run: python Automation/automation_new.py run
```

## 📝 Структура файлов

```
Automation/
├── automation_new.py      # Главный скрипт
├── build_config.py        # Конфигурация и настройки
├── build_actions.py       # Действия сборки
├── build_menu.py          # Интерактивное меню
├── build_new.bat          # Wrapper для Windows
└── BUILD_SYSTEM_README.md # Эта документация
```

## 🆚 Сравнение со старой системой

| Функция                      | Старая система | Новая система        |
|------------------------------|----------------|----------------------|
| Интерактивное меню           | ❌              | ✅                    |
| Поддержка нескольких IDE     | ❌              | ✅                    |
| Выбор системы сборки         | Только Ninja   | Ninja, MSBuild, Make |
| Отдельные папки сборки       | ❌              | ✅                    |
| Автообнаружение инструментов | ❌              | ✅                    |
| Цветной вывод                | ❌              | ✅                    |
| Удобная документация         | ❌              | ✅                    |

## 🐛 Решение проблем

### CMake не найден

```batch
# Установите CMake
# https://cmake.org/download/

# Или через chocolatey
choco install cmake
```

### Ninja не найден

```batch
# Установите Ninja
# https://ninja-build.org/

# Или через chocolatey
choco install ninja
```

### Visual Studio не найден

- Убедитесь, что установлена Visual Studio 2019 или 2022
- Установите компоненты "Desktop development with C++"
- Система автоматически найдет компиляторы MSVC

### Ошибки компиляции

```batch
# Попробуйте пересборку
build_new.bat rebuild

# Или очистите все и соберите заново
build_new.bat clean-all
build_new.bat generate
build_new.bat build
```

## 📚 Дополнительная информация

Для получения справки:

```batch
build_new.bat help
build_new.bat --help
python automation_new.py --help
```

## 🤝 Вклад в разработку

При внесении изменений:

1. Форматируйте код: `build_new.bat format`
2. Проверьте сборку: `build_new.bat rebuild`
3. Протестируйте разные конфигурации

---

**Версия**: 2.0  
**Дата**: 2025-01-13  
**Автор**: HuyEngine Team

