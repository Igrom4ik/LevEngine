# Disabled: Linux build removed from project CI per project policy (Windows-only CI)
# Keep file for reference; job is intentionally skipped.

name: Linux Build (Disabled)

on:
  workflow_dispatch:

jobs:
  build-linux:
    if: false
    name: Build on Linux (disabled)
    runs-on: ubuntu-latest

    steps:
      - name: Disabled
        run: |
          echo "Linux build disabled for this repository."
