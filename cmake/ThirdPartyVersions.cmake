# ...new file...
# Centralized third-party version pins for LevEngine
# Keep tags/commit hashes here so builds are reproducible and suitable for committing to git.

# Minimum CMake version required for this project (kept as a string for reference)
set(LEVENGINE_CMAKE_MINIMUM_VERSION "3.31.6")

# GLFW
set(LEVENGINE_GLFW_GIT_REPOSITORY "https://github.com/glfw/glfw.git")
set(LEVENGINE_GLFW_GIT_TAG "3.4")

# GLEW
set(LEVENGINE_GLEW_GIT_REPOSITORY "https://github.com/nigels-com/glew.git")
set(LEVENGINE_GLEW_GIT_TAG "glew-2.2.0")

# Future third-party pins can be added here, for example:
# set(LEVENGINE_SOMELIB_GIT_REPOSITORY "https://...")
# set(LEVENGINE_SOMELIB_GIT_TAG "v1.2.3")

