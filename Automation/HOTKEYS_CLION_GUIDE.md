# Настройка горячих клавиш для Build Menu в CLion

> ⚠️ Документ упрощён. Актуальная краткая инструкция перенесена в: `docs/CLION_HOTKEYS.md`.
> Рекомендуем использовать её. Ниже сохранены дополнительные подробности и диагностика.

## Быстрый старт (ссылка)

- Откройте: [docs/CLION_HOTKEYS.md](../docs/CLION_HOTKEYS.md)

## ❗ Почему Ctrl+Numpad1 не работает?

### Основные причины:

1. **External Tool настроен, но НЕ назначена горячая клавиша**
    - Вы создали External Tool в Settings → Tools → External Tools
    - Но не назначили горячую клавишу в Settings → Keymap
    - **Решение:** Следуйте инструкции ниже

2. **Num Lock выключен**
    - Клавиши на цифровой клавиатуре работают только с включенным Num Lock
    - **Решение:** Включите Num Lock и попробуйте снова

3. **Конфликт с другой горячей клавишей**
    - В CLion уже есть действие на Ctrl+Numpad1
    - **Решение:** Проверьте конфликты в Keymap → Find Shortcut

4. **CLion не перезагружен после настройки**
    - Изменения могут не применяться сразу
    - **Решение:** File → Invalidate Caches / Restart

5. **Неправильная конфигурация External Tool**
    - Program указывает на .bat напрямую вместо cmd.exe
    - **Решение:** Файл уже исправлен, перезагрузите CLion

## ✅ Быстрая диагностика

Запустите скрипт диагностики:

```batch
.\Automation\diagnose_hotkeys.bat
```

Он проверит все необходимые компоненты и подскажет, что нужно исправить.

## Проблема решена! ✅

Горячие клавиши теперь работают для запуска build_menu.bat через External Tools.

## Шаг 1: Настройка External Tool

### Настройка в CLion

1. Откройте **Settings** → **Tools** → **External Tools**
2. Нажмите **+** (Add)
3. Заполните поля:

**Name**: `HuyEngine Build Menu`

**Description**: `Запуск интерактивного меню сборки в отдельном окне PowerShell 7`

**Program**:

```
C:\Windows\System32\cmd.exe
```

**Arguments**:

```
/c start "HuyEngine Build" pwsh.exe -NoExit -Command "Set-Location '$ProjectFileDir$'; python '.\Automation\build_menu.py'"
```

**Working directory**:

```
$ProjectFileDir$
```

**Дополнительные настройки:**

- ☐ Synchronize files after execution (можно оставить выключенным)
- ☐ Open console for tool output (можно оставить выключенным, так как открывается отдельное окно)

4. Нажмите **OK**

✅ Правильная конфигурация! Теперь скрипт будет запускаться в **отдельном окне PowerShell 7** с полной поддержкой
интерактивного ввода и ANSI-цветов.

## Шаг 2: Назначение горячей клавиши Ctrl+Numpad1

1. **Откройте настройки Keymap**:
    - `File` → `Settings` (или `Ctrl+Alt+S`)
    - В левой панели выберите `Keymap`

2. **Найдите ваш External Tool**:
    - В строке поиска введите: `HuyEngine Build Menu`
    - Или разверните: `External Tools` → `External Tools` → `HuyEngine Build Menu`

3. **Добавьте горячую клавишу**:
    - Щелкните правой кнопкой мыши на `HuyEngine Build Menu`
    - Выберите `Add Keyboard Shortcut`
    - В появившемся окне нажмите: `Ctrl` + `Numpad 1`
    - Если появится предупреждение о конфликте, выберите `Remove` для удаления конфликтующей привязки
    - Нажмите `OK`

4. **Примените изменения**:
    - Нажмите `Apply`
    - Нажмите `OK`

## Шаг 3: Проверка работы

1. Откройте любой файл в редакторе CLion
2. Нажмите `Ctrl+Numpad1`
3. Должно открыться **новое окно PowerShell 7** с интерактивным меню сборки

## Что происходит при нажатии Ctrl+Numpad1?

1. CLion запускает `cmd.exe`
2. `cmd.exe` выполняет команду `start`, которая открывает новое окно PowerShell 7
3. PowerShell переходит в директорию проекта
4. Запускается Python скрипт `build_menu.py`
5. Окно остаётся открытым после выполнения (флаг `-NoExit`)

## Преимущества данного подхода

✅ **Работает независимо от расположения проекта** — использует макрос `$ProjectFileDir$`  
✅ **Открывается в отдельном окне** с полной поддержкой интерактивного ввода  
✅ **Поддерживает ANSI-цвета и Unicode** — красивый вывод меню  
✅ **Не блокирует работу CLion** — можно продолжать работать в IDE  
✅ **Сохраняет историю** — окно остаётся открытым после завершения скрипта

## Требования

- **PowerShell 7** (`pwsh.exe`) должен быть установлен и доступен в PATH
- **Python 3.x** должен быть установлен и доступен в PATH
- Скрипт `Automation/build_menu.py` в корне проекта

## Альтернативные горячие клавиши

Если `Ctrl+Numpad1` не подходит, попробуйте назначить другую комбинацию:

- `Ctrl+Shift+B` — Build Menu
- `Alt+B` — Build Menu
- `Ctrl+Alt+B` — Build Menu
- `F12` — Build Menu
- `Ctrl+1` — если NumPad не работает, используйте обычную цифру

## Альтернативный способ: через Windows Terminal

Если у вас установлен Windows Terminal и вы хотите использовать его вместо стандартного окна PowerShell:

**Program**:

```
C:\Users\<ВАШ_USERNAME>\AppData\Local\Microsoft\WindowsApps\wt.exe
```

**Arguments**:

```
-w 0 new-tab --title "HuyEngine Build" pwsh.exe -NoExit -Command "Set-Location '$ProjectFileDir$'; python '.\Automation\build_menu.py'"
```

**Working directory**:

```
$ProjectFileDir$
```

> **Примечание**: Замените `<ВАШ_USERNAME>` на ваше имя пользователя Windows или используйте полный путь к `wt.exe`.

## Устранение неполадок

### Ошибка: "Cannot run program"

Если появляется ошибка `Cannot run program "%SystemRoot%\System32\cmd.exe"`:

- Используйте полный путь: `C:\Windows\System32\cmd.exe`

### Ошибка: "The system cannot find the file specified"

Если появляется ошибка `error 2147942402 (0x80070002)`:

- Убедитесь, что `pwsh.exe` установлен и доступен в PATH
- Проверьте, что `python` установлен и доступен в PATH
- Убедитесь, что скрипт `Automation/build_menu.py` существует

### Горячая клавиша не срабатывает

1. Проверьте, что горячая клавиша назначена в `Settings` → `Keymap`
2. Убедитесь, что нет конфликтов с другими действиями
3. Перезапустите CLion: `File` → `Invalidate Caches / Restart`
4. Убедитесь, что Num Lock включён (для NumPad клавиш)

## Настройка дополнительных инструментов

Вы также можете создать дополнительные External Tools для других команд:

### Quick Generate - Быстрая генерация проекта

- **Name**: `Quick Generate`
- **Program**: `C:\Windows\System32\cmd.exe`
- **Arguments**:
  `/c start "Generate" pwsh.exe -NoExit -Command "Set-Location '$ProjectFileDir$'; python -c 'from Automation.build_actions import generate_project; generate_project()'"`
- **Горячая клавиша**: `Ctrl+Numpad2`

### Quick Build - Быстрая сборка

- **Name**: `Quick Build`
- **Program**: `C:\Windows\System32\cmd.exe`
- **Arguments**:
  `/c start "Build" pwsh.exe -NoExit -Command "Set-Location '$ProjectFileDir$'; python -c 'from Automation.build_actions import build_project; build_project()'"`
- **Горячая клавиша**: `Ctrl+Numpad3`
- **Working directory**: `$ProjectFileDir$`
- **Горячая клавиша**: `Ctrl+Numpad2` или `F9`

### Generate (Quick) - Быстрая генерация

- **Name**: `Generate (Quick)`
- **Program**: `%SystemRoot%\System32\cmd.exe`
- **Arguments**: `/c "$ProjectFileDir$\Automation\build.bat" generate`
- **Working directory**: `$ProjectFileDir$`
- **Горячая клавиша**: `Ctrl+Numpad3` или `Ctrl+F9`

### Clean (Quick) - Быстрая очистка

- **Name**: `Clean (Quick)`
- **Program**: `%SystemRoot%\System32\cmd.exe`
- **Arguments**: `/c "$ProjectFileDir$\Automation\build.bat" clean`
- **Working directory**: `$ProjectFileDir$`
- **Горячая клавиша**: `Ctrl+Numpad4` или `Shift+F9`

### Rebuild (Quick) - Быстрая пересборка

- **Name**: `Rebuild (Quick)`
- **Program**: `%SystemRoot%\System32\cmd.exe`
- **Arguments**: `/c "$ProjectFileDir$\Automation\build.bat" rebuild`
- **Working directory**: `$ProjectFileDir$`
- **Горячая клавиша**: `Ctrl+Numpad5` или `Ctrl+Shift+F9`

## Расширенные настройки (Advanced Options)

В разделе "Advanced Options" можно настроить:

- ✅ **Synchronize files after execution** - синхронизация файлов после выполнения
- ✅ **Open console for tool output** - открыть консоль для вывода
- ✅ **Make console active on message in stdout** - активировать консоль при выводе
- ⬜ **Make console active on message in stderr** - активировать консоль при ошибках
- ⬜ **Collapse console on tool finish** - свернуть консоль после завершения

Рекомендуется включить первые три опции.

## Использование макросов CLion

В настройках External Tools доступны следующие макросы:

- `$ProjectFileDir$` - путь к корню проекта
- `$FilePath$` - путь к текущему файлу
- `$FileDir$` - директория текущего файла
- `$FileName$` - имя текущего файла
- `$FileNameWithoutExtension$` - имя файла без расширения
- `$ContentRoot$` - корень содержимого проекта
- `$ModuleFileDir$` - директория файла модуля

## Troubleshooting

### Проблема: Горячая клавиша не работает

**Решение 1**: Проверьте конфликты

1. `Settings` → `Keymap`
2. Нажмите на иконку 🔍 поиска
3. В выпадающем меню выберите `Find Shortcut`
4. Нажмите `Ctrl+Numpad1`
5. Проверьте, нет ли конфликтов с другими действиями

**Решение 2**: Перезагрузите CLion

1. `File` → `Invalidate Caches / Restart`
2. Выберите `Invalidate and Restart`

**Решение 3**: Используйте другую горячую клавишу

- Попробуйте `Ctrl+Alt+B` или `F12`

### Проблема: Окно консоли не открывается

**Решение**: Включите опцию "Open console for tool output"

1. `Settings` → `Tools` → `External Tools`
2. Выберите `Build Menu`
3. Разверните `Advanced Options`
4. ✅ Включите `Open console for tool output`
5. ✅ Включите `Make console active on message in stdout`

### Проблема: Скрипт не запускается

**Решение**: Проверьте пути

1. Убедитесь, что файл существует: `C:\DEVOPS\HuyEngine\Automation\build_menu.bat`
2. Проверьте, что Python установлен: `python --version`
3. Запустите скрипт вручную из терминала:
   ```batch
   cd C:\DEVOPS\HuyEngine
   .\Automation\build_menu.bat
   ```

### Проблема: "Python not found"

**Решение**: Установите Python или добавьте в PATH

1. Скачайте Python: https://www.python.org/downloads/
2. При установке включите "Add Python to PATH"
3. Или добавьте Python в PATH вручную:
   ```batch
   setx PATH "%PATH%;C:\Python310"
   ```

## Карта горячих клавиш (рекомендуемая)

| Горячая клавиша | Действие         | Описание                 |
|-----------------|------------------|--------------------------|
| `Ctrl+Numpad1`  | Build Menu       | Интерактивное меню       |
| `Ctrl+Numpad2`  | Build (Quick)    | Быстрая сборка           |
| `Ctrl+Numpad3`  | Generate (Quick) | Быстрая генерация        |
| `Ctrl+Numpad4`  | Clean (Quick)    | Быстрая очистка          |
| `Ctrl+Numpad5`  | Rebuild (Quick)  | Быстрая пересборка       |
| `Ctrl+Numpad6`  | Run (Quick)      | Запуск приложения        |
| `F9`            | Build            | Стандартная сборка CLion |
| `Shift+F10`     | Run              | Стандартный запуск CLion |

## Альтернатива: Run Configuration

Если External Tools не работают, можно создать Run Configuration:

1. `Run` → `Edit Configurations...`
2. Нажмите `+` → `Shell Script`
3. **Name**: `Build Menu`
4. **Script path**: `C:\DEVOPS\HuyEngine\Automation\build_menu.bat`
5. **Working directory**: `C:\DEVOPS\HuyEngine`
6. **Execute in the terminal**: ✅
7. Нажмите `OK`

Теперь можно:

- Запускать через `Shift+F10`
- Добавить в Toolbar
- Назначить свою горячую клавишу через Keymap → Run Configuration → Build Menu

## Видео-инструкция (текстовая версия)

### Часть 1: Настройка External Tool

```
1. Settings (Ctrl+Alt+S)
   ↓
2. Tools → External Tools
   ↓
3. Нажать "+" (Add)
   ↓
4. Заполнить поля:
   Name: Build Menu
   Program: cmd.exe
   Arguments: /c "$ProjectFileDir$\Automation\build_menu.bat"
   Working directory: $ProjectFileDir$
   ↓
5. OK
```

### Часть 2: Назначение горячей клавиши

```
1. Settings (Ctrl+Alt+S)
   ↓
2. Keymap
   ↓
3. Найти: External Tools → Build Menu
   ↓
4. ПКМ → Add Keyboard Shortcut
   ↓
5. Нажать: Ctrl+Numpad1
   ↓
6. OK → Apply → OK
```

### Часть 3: Использование

```
1. Нажать Ctrl+Numpad1
   ↓
2. Открывается терминал с Build Menu
   ↓
3. Выбрать действие из меню
   ↓
4. Готово!
```

## Дополнительные ресурсы

- [JetBrains Documentation - External Tools](https://www.jetbrains.com/help/clion/configuring-third-party-tools.html)
- [JetBrains Documentation - Keymap](https://www.jetbrains.com/help/clion/configuring-keyboard-and-mouse-shortcuts.html)
- [Automation README](README.md)
- [Build System Documentation](BUILD_SYSTEM_README.md)

## Успех! 🎉

После выполнения всех шагов вы сможете:

✅ Нажимать `Ctrl+Numpad1` для открытия Build Menu  
✅ Быстро собирать проект из любого места в CLion  
✅ Использовать горячие клавиши для других команд сборки  
✅ Настраивать собственные комбинации клавиш

**Приятной работы!** 🚀
